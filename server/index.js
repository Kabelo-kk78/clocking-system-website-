// Vercel serverless entry point.
// Vercel builds this file with @vercel/node; every function invocation
// reuses the warm Express app. Local development uses server.js instead.
import app from './app.js'

export default app